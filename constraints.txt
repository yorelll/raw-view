# Dependency version lock file.
# Used by CI via `pip install -c constraints.txt` on top of requirements.txt
# to make builds reproducible. Keep this file ASCII-only: pip reads it with
# the platform default encoding, and non-ASCII bytes can break parsing on
# GBK/cp1252 Windows runners.
#
# Versions here match the combination validated locally (Python 3.12 / Windows).
# Local installs still use only `pip install -r requirements.txt`; constraints
# are optional.

numpy==2.4.4
opencv-python==4.13.0.92
PyQt5==5.15.11
qt-material==2.17
qtawesome==1.4.2
Pillow==12.3.0
pyinstaller==6.20.0
pyinstaller-hooks-contrib==2026.4
